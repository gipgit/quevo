document.addEventListener('DOMContentLoaded', function() {
    /*
    const topnav = document.getElementById('topnav');

    // Get the computed style of the topnav element
    const style = window.getComputedStyle(topnav);

    // Get the margin-bottom value (it will be a string like "10px")
    const marginBottom = parseFloat(style.marginBottom); // Parse to a number

    // Calculate total height including margin-bottom
    const topnavHeight = topnav.offsetHeight + marginBottom + 30;

    document.getElementById("main").style.paddingTop = topnavHeight + 'px';
    */
});